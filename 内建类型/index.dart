/**
 * Dart 语言支持以下内建类型:
 * Number
 * String
 * Boolean
 * List (也被称为 Array)
 * Map
 * Set
 * Rune (用于在字符串中表示 Unicode 字符)
 * Symbol
 * 这些类型都可以被初始化为字面量。 
 * 例如, 'this is a string' 是一个字符串的字面量， true 是一个布尔的字面量。
 * 因为在 Dart 所有的变量终究是一个对象（一个类的实例）， 所以变量可以使用 构造涵数 进行初始化。 
 * 一些内建类型拥有自己的构造函数。 例如， 通过 Map() 来构造一个 map 变量。
 */
